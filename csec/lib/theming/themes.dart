import 'package:csec/colors_dimensions/colors.dart';
import 'package:flutter/material.dart';

ThemeData lightMode = ThemeData(
    brightness: Brightness.light, primaryColor: ColorsHome.mainBlackColor);

ThemeData darkMode =
    ThemeData(brightness: Brightness.dark, primaryColor: ColorsHome.iconColor2);
